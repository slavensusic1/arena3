<!-- ---
const {text, title, img,category, link,transitionName,transitionNameTitle}= Astro.props
import { Image } from "astro:assets";

---


<div class="max-w-sm mx-auto rounded-xl shadow-md bg-white dark:bg-gray-800 overflow-hidden opacity-90">
    <a href={link}>
      <Image transition:name={transitionName} class="w-full object-cover" src={img} alt="Card image" />
    </a>
    <div class="p-6">
      <span class="uppercase tracking-wide text-xs font-semibold text-green-600 dark:text-green-400 bg-green-100 dark:bg-green-800 px-2 py-1 rounded-full mb-2 inline-block">{category}</span>
      <h4 class="text-2xl font-semibold text-gray-900 dark:text-white transition-name-title" transition:name={transitionNameTitle}>{title}</h4>
      <p class="text-gray-500 dark:text-gray-300 mt-2 text-sm">{text}.</p>
    </div>
  </div>
   -->