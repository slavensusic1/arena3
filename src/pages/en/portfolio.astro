---
import PortfolioLayout from "../../layouts/PortfolioLayout.astro";
import Portfolio from "../../components/portfolio/Portfolio.jsx";

import { languages } from '../../i18n/ui';
import { getLangFromUrl, useTranslations, useTranslatedPath } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const translatePath = useTranslatedPath(lang);
---

<PortfolioLayout>
<Portfolio client:load lang={lang} headline={t('portfolio.headline')} subHeadline={t('portfolio.subHeadline')} apps={t('portfolio.apps')}
    miss={t('portfolio.Miscellaneous')} banksTitle={t('banks.title')} energetics={t('banks.listitem1')} 
    banksListItem={t('banks.listitem1')} banks2={t("Banks.2")}
    banks3={t("Banks.3")}
    banks4={t("Banks.4")}
    banks5={t("Banks.5")}
    banks6={t("Banks.6")}
    d1= {t("d.sector.title")}
    d2= {t("d.sector.1")}
    d3= {t("d.sector.2")}
    d4= {t("d.sector.3")}
    t0={t("Trade.title")}
   t1={t("Trade.1")}
  t2= {t("Trade.2")}
  t3={t("Trade.3")}
   t4={t("Trade.4")}
   t5={t("Trade.5")} 
   t6={t("Trade.6")}  
   t7={t("Trade.7")}  
   t8={t("Trade.8")}  
   te1={t("tele.title")} 

   te2={t("tele.1")} 
   te3={t("tele.2")} 

   te4={t("tele.3")} 
 
    
    />
</PortfolioLayout>



